# --- Secrets ---
include/secrets.h

# --- ESP-IDF / PlatformIO build ---
.pio/
sdkconfig
sdkconfig.*
managed_components/
dependencies.lock

# --- Local notes / context (kept off GitHub) ---
CONTEXT.md
*_CONTEXT.md

# --- Datasheets / large binaries (kept local) ---
*.pdf
*.tar.gz

# --- Linux / CMake build (pi5 port) ---
build/
build-arm64/
*.o
*.a
*.so

# --- Editor / OS ---
.vscode/
.DS_Store

# --- Local scratch / turds (kept local, never committed) ---
foo
deleteme
RUNME
evj55_splash_c.h
