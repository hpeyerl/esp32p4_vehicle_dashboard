// =============================================================
//  layout_stub.h — Dashboard geometry for stub/headless build
//  Uses Waveshare layout (target hardware).
// =============================================================
#pragma once
#include "layout_waveshare.h"
